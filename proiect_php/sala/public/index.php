


<!-- <?php
// Load the Router
require_once "config/routes.php";


echo "Am ajuns în public/index.php <br>";

// Start the session (optional, if using sessions)
session_start();

// Get the current URI
$uri = trim($_SERVER["REQUEST_URI"], "/");

echo $uri;

// Initialize the router and route the request
$router = new Router();
$router->direct($uri);
?> -->

<h1>aloooo</h1>